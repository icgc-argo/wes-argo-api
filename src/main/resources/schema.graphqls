type Donor {
    donorId: String!
    submittedId: String!
}

type Analysis {
    analysisId: String!
    analysisTypeVersion: Int!
    analysisTypeName: String!
    donors: [Donor]!
}

type Query {
    donorById(donorId: String!): Donor
    analysisById(analysisId: String!): Analysis
}
